[project]
name = "plov"
version = "0.1.0"
description = "Telegram-бот на Django, который отправляет случайные фото плова"
requires-python = ">=3.9"
dependencies = [
    "django>=4.2.30",
    "requests>=2.31.0",
    "gunicorn>=21.0.0",
]
